// 4) To make the program compile, you need to replace the placeholder "DATA_TYPES_PLEASE" with the appropriate data types in the given program. Modify the program by assigning the correct data types to the variables to ensure compilation.

fn main() {
    let a: i16 = -15;

    let b: i16 = 170;

    let my_name: &str = "Michael";

    println!(
        "My name is {}, and the multiplication result is {}",
        my_name,
        a * b
    );
}
